import { LoadingScreen } from "@/components/LoadingScreen";

export default function RenterProfileLoading() {
  return <LoadingScreen title="Loading renter profile" subtitle="Preparing bookings, tracking, and call actions..." />;
}
