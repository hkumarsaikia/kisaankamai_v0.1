import { LoadingScreen } from "@/components/LoadingScreen";

export default function ProfileSelectionLoading() {
  return <LoadingScreen title="Loading workspaces" subtitle="Preparing owner and renter profile selection..." />;
}
