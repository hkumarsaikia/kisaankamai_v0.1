import { LoadingScreen } from "@/components/LoadingScreen";

export default function SupportLoading() {
  return <LoadingScreen title="Loading support" subtitle="Preparing hubs, maps, and support channels..." />;
}
