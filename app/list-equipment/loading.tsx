import { LoadingScreen } from "@/components/LoadingScreen";

export default function ListEquipmentLoading() {
  return <LoadingScreen title="Loading equipment editor" subtitle="Preparing listing fields and live preview..." />;
}
