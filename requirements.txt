# Kisan Kamai workspace Python requirements.
#
# The tracked Python utility scripts currently use only the Python standard
# library. Keep this file present so Python setup commands have a canonical
# target, and add pinned third-party packages here only when a tracked Python
# script actually requires them.
