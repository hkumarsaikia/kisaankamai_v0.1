# Decisions

- The shared launcher name is `codex-antigravity-sync`.
- Current live conversation context is always preferred over persisted local session surfaces.
- Only explicitly referenced non-current conversations are ingested automatically.
- Antigravity `.pb` files are recorded as opaque binary artifacts unless a future schema is added.
